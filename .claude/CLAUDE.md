# Project: <PROJECT_NAME>

> One-line description: <WHAT_THIS_PROJECT_DOES>

This file is loaded in every Claude Code session in this project. It supplements (not replaces) the global preferences in `~/.claude/CLAUDE.md`.

## Stack

- **Language:** <e.g. TypeScript / Python / Go>
- **Framework:** <e.g. Next.js / FastAPI / Cobra>
- **Database:** <e.g. Postgres / SQLite / none>
- **Deploy target:** <e.g. Vercel / Fly.io / TBD>
- **Test framework:** <e.g. vitest / pytest / go test>

## Commands

| Purpose | Command |
|---------|---------|
| Install deps | `<TODO>` |
| Run dev server | `<TODO>` |
| Run tests | `<TODO>` |
| Run lint | `<TODO>` |
| Run formatter | `<TODO>` |
| Build for production | `<TODO>` |

## Project conventions

- <Add conventions specific to this codebase. E.g.: "All API routes return JSON. Errors use `{ error: { code, message } }` shape.">
- <E.g.: "Database access is centralized in `src/db/`. Don't import the ORM elsewhere.">

## Do not touch

- `.github/workflows/` — modifying CI changes how this repo verifies safety. Ask before editing.
- `decisions.md` — append-only log. Never edit existing entries.
- <Add other "do not touch" zones>

## Where to find context

- Architecture: `docs/architecture.md`
- Past decisions: `docs/decisions.md`
- This project's GitHub: `https://github.com/shmangopods/<PROJECT_NAME>`

## TODO before first session

Replace every `<PROJECT_NAME>`, `<TODO>`, and `<...>` placeholder above. Delete this section once done.
